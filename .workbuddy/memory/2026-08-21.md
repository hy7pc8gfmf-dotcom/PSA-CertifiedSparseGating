# 2026-08-21

## 任务：对 PSA-CertifiedSparseGating 跑 GitHub Actions CI

- CI 定义：`.github/workflows/coq.yml` → `coq-community/docker-coq-action@v1.5.2`，Rocq 9.0，运行 `scripts/ci_build.sh`。触发器仅 `push` 到 `main` / `pull_request`（**无 workflow_dispatch**）。
- 已推送空触发提交 `48c18b9`（RUN 32454526305，最终失败）—— 仅用于触发流水线。
- **3 次历史 CI 全部失败**（RUN 32451760436 / 32452140268 / 32452223997），根因在 `scripts/ci_build.sh` 自身两处 bug：
  1. `scripts/order.txt` 首行为 `#` 注释，`while read` 循环未跳过 → 把注释当文件名传给 `coqc` → `Can't find file ./coq/lib/# lib/ 编译顺序….v`。
  2. mathcomp/Coquelicot 路径解析错误：`ocamlfind query mathcomp` 在 Rocq 镜像里返回空 → 回退到不存在的 `/usr/lib/ocaml/mathcomp` → `Cannot open`；且 `Coquelicot` 目录名在大小写敏感的 Linux runner 上须为小写 `coquelicot`。
- **已修复** `scripts/ci_build.sh`（提交 `1419c63`，+32/−7）：分层解析路径（`coqc -where user-contrib` → `ocamlfind` → `opam var lib` 目录搜索 → 兜底），并在读 `order.txt` 时跳过 `#` 注释与空行。
- 提交 `1419c63` **尚未推送**：网络持续 `Recv failure: Connection was reset`（用户所说「撞墙」，约 10 分钟窗口）。已启动后台重试推送循环（task `sFc6rM`），命中稳定窗口即推送并触发新 CI run。
- 认证方式：`gh` CLI 未安装；`git-credential-manager`(GCM) 在本机 **segfault**；改用 `git -c credential.helper=wincred push origin main`（走 Windows 凭据管理器，GitHub Desktop 已存 token）可成功认证。
- 待办：推送成功后监控新 run 是否通过（若 mathcomp/coquelicot 实际仍找不到，会暴露真实路径，需再迭代）；可考虑为 `coq.yml` 增加 `workflow_dispatch` 便于手动重跑。
