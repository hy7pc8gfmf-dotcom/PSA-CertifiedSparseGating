# 全量交接文档（2026-08-19 · 会话 9）

> **用途**：新对话框立即接管。**先读第一节（温控）——4 次宕机的硬知识，勿重蹈。**
> **状态**：multi-seed 链 8/8 完成（exit 0）；M4b 长度一致性 + T8 复合证书 + frame_check_instance 全部并入 + 提取链落地；诚实性支柱一/二已跑。审计计数已并入 **131 项**（FrameCheckInstance soundness 4 项，PSA_audit RC=0 复核）。**框架 PSA_framework.v 全程零 Admitted**（grep `Admitted` 仅命中 2 处注释，无战术调用）。
> **GPU 现况（交接瞬时）**：实测 **55°C（凉，低于空闲基线 56-58°C）**；无论何时跑训练先 nvidia-smi 确认 <60°C。

---

## 〇、一句话状态

- **实验侧**：multi-seed（4 梯 × 3 seed {1337,42,7}）全完成——**E5'' 与 C=4 并列冠军**（8× 均值 12.40 vs 12.75，差 -2.8% 在噪声带），C=2 第三（13.84），**C=3 系统性最差**（22.86，归因=间距缺陷主导 + 方差放大，非同纯噪声）。psi-rope 可认证冠军仍是 C=4。
- **形式化侧**：M4 收尾①（长度一致性 `certified_c4_frame_bounds_anyN`）✅ + T8 复合证书（[3,15,63,255] 核）✅ + frame_check_instance 反射检查器入提取链 ✅ + FrameCheckInstance soundness（pair_sound 等 4 项）✅ → 审计 **131 项**（111+20 新增），**新增项全零 classic**，RC=0。
- **诚实性**：rope-NTK 反超 psi-rope 21.6%（闸门触发，主张收缩）；NoPE 盘内差（闸门确认，位置必要）。

---

## 一、⚠️ 温控协议 v3.2（第一必读——勿重蹈 4 次宕机）

同 }全量交接文档-20260819-会话8-强制终止版.md{ §一，不重复。要点：
- 空闲基线 56-58°C（交接瞬时实测 55°C，凉），死机临界 ~85°C。GPU 80°C 触发 / 70°C 恢复 / 0.5s 线程探测。
- 一次只跑一件事（训练 OR Coq，不同时——coqc 加 CPU 热）。
- 训练每步 `guard.check()`，日志落盘，>85°C 立即杀。
- **关键**：多 seed 8 run 全程零宕机、零失败（v3.2 验证再次通过），85°C 峰值出现但 0.5s 即时冷却存活。

---

## 二、multi-seed 主表（本会话核心产出，全程可靠）

**8 run 全部完成（exit 0，每 run ~5 分钟，GPU 温控全程正常）**。数据见 `psa_empirical\multi_seed_main_table.md`。汇总：

| 梯子 | @4096 均值 | stdev | 判定 |
|------|-----------|-------|------|
| E5'' [3,7,..255] | **12.40** | 0.735 | 并列冠军 |
| C=4 [3,13,53,213] | 12.75 | 0.345 | 并列冠军（直接可认证） |
| C=2 [3..511] | 13.84 | 0.247 | 稳定第三 |
| C=3 [3,10,31,94,283] | **22.86** | 5.304 | **系统性最差** |

**科学裁决（诚实）**：
1. **冠军并列**：E5''/C=4 差 -2.8% 在噪声带内（seed 7 时 E5'' 11.55 全场最佳）。论文 B 主表用"双冠军"表述 + 复合证书对冲。
2. **C=3 归因定案**：间距缺陷主导。三 seed 全大幅劣于其余（好 seed 也 >19.4 vs <14.1），且方差被极度放大（5.30 vs 0.25-0.74）——"间距一半在均值、一半在方差"。s1337 的 28.97 是坏抽签。
3. **覆盖梯度 3-seed 确认**：八带 → 剪 511 带（七带）全部 OOD 长度全部 seed 严格改善。

**模型文件**：`model_psi-rope_b{512}_c{2,3}_s{42,7}.pt`、`model_psi-rope_b512_s{42,7}.pt`、`model_psi-rope_b512_c2_bands7_s{42,7}.pt`。C=4 s1337 无后缀（`model_psi-rope_b512.pt`）。

---

## 三、形式化侧（会话 9 全部真证并入，审计 131 项 RC=0）

### 3.1 PSA_framework.v（3107 → 3671 行，新增 3 模块）
| 模块 | 内容 | 审计 |
|------|------|------|
| **M4bLengthConsistency** | 长度一致性：`certified_c4_frame_bounds_anyN` ∀N≥214 + 4 窗口参数化行和 | 5 项全零 classic |
| **T8CoreCertificate** | T8 复合证书核 [3,15,63,255]：`certified_t8_core_frame_bounds` μ=4/5 + 6 对界 + 4 行和 | 11 项全零 classic |
| **FrameCheckInstance** | 反射检查器 `frame_check_instance`（任意梯子 → μ≤4/5 布尔判定）+ `pair_ok` + **`pair_sound`（soundness，Qed）** | 定义 + 1 项 soundness |

- 备份：`PSA_framework_backup_20260819_M4pre.v`（含 M4b/T8 并入前）。
- 审计：`PSA_audit.v` **131 项**（127 + FrameCheckInstance soundness 4 项），`audit_final2.txt` 追加会话 9 两段。新增项全零 classic（仅继承 sig_not_dec+sig_forall_dec+fext）。RC=0 ✓。

### 3.2b M4c soundness 进展（会话 9 末 + 续）

> **审计现况（已落盘复核）**：PSA_audit.v **131 项**（131 个 `Print Assumptions`），编译 **RC=0**；其中 FrameCheckInstance soundness 4 项（pair_sound/div_le/frac_add_R/row_le_R）全部零 `classic`，仅继承 `sig_not_dec + sig_forall_dec + functional_extensionality_dep`。**框架 PSA_framework.v 零 `Admitted`**（grep 命中 2 处均为注释）。完整复核见 `audit_final2.txt` 末尾两段。

- **已 Qed（并入 PSA_framework.v FrameCheckInstance 模块）**：
  - `pair_sound`：pair_ok ⟹ 反射有理界 ≥ psi 内积保守界
  - `div_le`：除法不等式交叉相乘（a·d ≤ c·b ⟹ a/b ≤ c/d，b,d≥0）
  - `frac_add_R`：反射分数加法 = R 分数加法（INR 同态）
  - `INR5`/`INR4`：INR 字面量计算
  - `row_le_R`：5·num ≤ 4·den ⟹ INR num/INR den ≤ 4/5
  （以上 INR 归一化技巧：`compute; ring` 证 INRn、mult_INR/plus_INR 展开、field+非零）
- **审计 131 项**（全量，RC=0，见上方现况框）。
- **`frame_check_instance_sound` 完整主定理仍未 Qed**（**下会话二选一开始选项 A**，§五）：
  需对任意梯子 I 归纳——(a) 逐对 pair_sound + Cnorm_Csum_conj_sym 对序 (min,max) →
  (b) row_sum_frac 累加 = 分数加法（frac_add_R）+ R 行和 ≤ INR(num)/INR(den) →
  (c) row_le_R ⟹ ≤ 4/5 → (d) psi_unit_norm → (e) gershgorin_frame_mu 通用实例化。
  子部件（8 引理）全 Qed；剩 (a)-(e) 的归纳装配。未引入 Admitted。
  **✅ 已装配（会话 10+11 完成）**：主定理 (a)-(e) 全部 Qed，审计 131 → 139 项全零 classic，
  见 `全量交接文档-20260819-会话10-frame装配.md` §三/四与 `audit_final2.txt` 末尾段。
- **M1.5 去 classic**（**下会话二选一开始选项 B**，§五）：级数路线已锁定（E1 每项非负 ⟹ E1 x N ≥ 1 ⟹ 极限 exp x ≥ 1 ⟹ 单调，
  零 classic）；`term_nonneg`/`E1_S` 探针骨架在本会话验证方向正确，完整 E1 proof 待专会话。

### 3.2 提取链（frame_check_instance 入链，24/24 自测通过）
- PSA_extract.v：新增提取 FrameCheckInstance 全部定义（含 `frame_check_instance`）。
- **Native int 镜像**：提取的 Peano nat 版 `frame_check_instance` 在 DkMLNative 字节码下对大梯子（≥7 带）栈溢出（大分母乘法展开海量 S）。**main.ml 用原生 int `frame_check_instance_int` 镜像 Coq 定义**，逻辑逐行同构，机器字整数无栈问题。key 梯子验证：C4/T8→true、E5''/C2b→false、2band→true，回归 19/19 OK。
- `psa_guard.exe` frame 命令 + `psa_guard_ffi.py` `frame_check_instance()` wrapper + 5 个 frame 自测项。
- **FFI 自测 24/24**（19 旧 + 5 新 frame）。

### 3.3 ⚠️ 审计勘误（如实传承）
- M1 3 项 + M2 1 项仍带 `Classical_Prop.classic`（exp 单调性经 exp_increasing/MVT 路线）——**这是遗留**，未在会话 9 消除。**M1.5 级数路线已验证可行**（psa_empirical 同级：exp_plus/exp_form/E1_cvg 全部零 classic，探针 `PSA_M15_probe.v` 记录了 `sum_incr`/`decomp_sum`/`cond_pos_sum` 证明骨架，但 exp_ineq1 也用 MVT 需级数重写）。
- 隔离探针保留：PSA_M4b_probe.v / PSA_T8_probe.v / PSA_M4c_probe.v / PSA_M15_probe.v（E039 纪律）。

---

## 四、诚实性支柱（两臂已跑，关键发现）

### 4.1 rope-NTK / rope-PI（eval-only 零训练成本）
| T | rope裸 | rope-NTK | rope-PI | psi-rope C4 |
|---|--------|---------|---------|-------------|
| 1024 | 6.76 | 5.11 | 16.59 | 5.71 |
| 2048 | 12.33 | 6.55 | 32.08 | 8.41 |
| 4096 | 20.28 | **10.74** | 37.87 | 13.06 |

**⚠️ 诚实性闸门触发**：rope-NTK @4096=10.74 领先 psi-rope(13.06) **21.6% > 15%** ⟹ 按设计判定树：**"psi-rope 免调参还赢"强主张被推翻**，主张收缩为"免手术 + 带证书 + 免逐长度选参"。论文 B 需重构为免维护+证书叙事。rope-PI 反常（37.87 差于裸绳）——char 级小模型不适用位置插值。

### 4.2 NoPE 控制（seed 42）
- 盘内 9.06 vs dense 5.40（差 3.66>1.5）⟹ **位置信息必要性确认**（闸门不报）。
- 外推极平缓（9.06→11.55 仅 1.27×）——无位置结构可碎，与位置方法"盘内好外推崩"对照，佐证位置必要。

---

## 五、当前任务队列（按优先级 + 下会话起步二选一）

> **下会话起步二选一（二选一即可，无需都做）**：
> - **【选项 A】主定理 (a)-(e) 装配**：完成 `frame_check_instance_sound` 唯一未 Qed 义务，闭合"证书闭环"主张。
>   **✅ 已于会话 10+11 完成**（审计 131 → 139，全零 classic）——无需再做。
> - **【选项 B】M1.5 E1 级数去 classic**：把审计 139 项中仅剩的 4 项 `classic` 依赖清零（exp 单调性改级数路线）。
> 两者互不阻塞，无依赖；按手感选其一推进即可，无需并行。

1. **【选项 A·已完成，会话 10+11】frame_check_instance_sound 主定理 Qed**（原唯一未 Qed 义务）：
   `pair_sound` + `div_le` + `frac_add_R` + `row_le_R` 已 Qed（共 8 个 soundness 子部件）；
   (a)-(e) 装配已全部 Qed（pair_bound_ok / row_sum_frac_aux_R / row_sum_frac_R_value /
   all_rows_le_forall / row_sum_frac_den_neq0 / frame_check_instance_row_bound /
   frame_check_instance_sound），审计 131 → **139 项全零 classic**，PSA_framework.v / PSA_audit.v RC=0。
   详见证 `全量交接文档-20260819-会话10-frame装配.md`。
2. **【选项 B】M1.5 去 classic**（清零审计仅剩 4 项 classic，升级为"139 项全零 classic"）：
   级数路线（E1 ∑ t^k/k! 正项 + sum_incr 取极限 ⟹ exp t ≥ 1 ⟹ 单调，零 classic）。
   探针骨架已备（`PSA_M15_probe.v` 的 `term_nonneg`/`E1_S`/`sum_incr`/`decomp_sum`/`cond_pos_sum` 方向已验证）；
   注意 `exp_ineq1` 也走 MVT 需一并级数重写。改完同步更新审计结论与 §3.3 勘误。
3. **诚实性两臂剩余**：E1 消融（psi-rand/lin/frozen，15 run，最耗时）——排队（依赖 GPU，先温控）。
4. **论文 B §6 假设证伪链**：C=3 裁决已定案，需更新"间距+方差双重"最终结论（§6 已部分更新，核对措辞）。
5. **论文对齐收尾**：A 的 §7 公理记账更新至 131 项；B 抽象/§4.1 措辞核对（含 NTK 反超后"免维护+证书"叙事）。

---

## 六、命令速查（本会话实测可用）

### 6.1 Coq 编译（PSA 框架）
```powershell
$dir = "C:\Rocq-Platform~9.0~2025.08\bin"; $uc = "$dir\..\lib\coq\user-contrib"
Push-Location $dir
& .\coqc.exe -R "D:\ComplexAnalysis\Live_harness\AI注意力算法" PSA -Q "D:\ComplexAnalysis\Live_harness\lib-构造性" "" -Q "$uc\mathcomp" mathcomp -Q "$uc\Coquelicot" Coquelicot "D:\ComplexAnalysis\Live_harness\AI注意力算法\PSA_framework.v"
```

### 6.2 OCaml 提取链（会话 9 新解——解决 stdlib 路径 bug）
```powershell
$base = Join-Path $env:LOCALAPPDATA "Programs\DkMLNative\desktop\bc"
$env:OCAMLLIB = "$base\lib\ocaml"          # ⚠️ 必须设！ocamlc -where 误指向 Y:\ 残余
$env:PATH = "$base\bin;$env:PATH"
Push-Location "D:\ComplexAnalysis\Live_harness\AI注意力算法"
ocamlc.exe -I "$base\lib\ocaml" psa_guard.ml psa_guard_main.ml -o psa_guard.exe
python psa_guard_ffi.py    # 24/24
```
**⚠️ 关键教训**：DkMLNative 的 ocamlc 默认 stdlib 路径指向坏掉的 `Y:/source/dkml/build/...`，必须设 `OCAMLLIB` 指向实际 `...\bc\lib\ocaml`，否则 `ocamlc -c` 报含糊的 "Unbound module Stdlib"。

### 6.3 训练 / NTK eval / NoPE
```powershell
# multi-seed（本会话已全完成；重跑需 fresh，见会话8 §6.4 脚本）
python length_extrap.py --iters 3000 --block 512 --gpu-max 80 --gpu-resume 70 --cpu-util-max 90 --check-interval 0.5 --cooldown 60 --batch 32 --seed S --gen-c N [--bands B] --modes psi-rope *> train_log_ms_xxx.txt
# rope-NTK / rope-PI eval-only（零训练成本，复用 model_rope_b512.pt）
python length_extrap.py --block 512 --eval-only rope --rope-variant ntk|pi
# NoPE（无位置项对照）
python length_extrap.py --modes nope --seed 42
```

---

## 七、会话 9 新增/变更文件清单

| 文件 | 变更 |
|------|------|
| `psa_empirical\multi_seed_main_table.md` | **新增**：四梯 3-seed 完整 4-T 主表 |
| `psa_empirical\train_log_ms_{c4,c2,e5pp,c3}_{s42,s7}.txt` | **新增**：8 run 完整日志 |
| `psa_empirical\eval_rope_ntk.txt` / `eval_rope_pi.txt` | **新增**：诚实性对照 |
| `psa_empirical\train_log_nope.txt` | **新增**：NoPE 对照 |
| `psa_empirical\length_extrap.py` | +`--rope-variant`、+`nope` 模式 |
| `PSA_framework.v` | 3107→3671 行，+M4b/T8/FrameCheckInstance 三模块 |
| `PSA_framework_backup_20260819_M4pre.v` | **新增备份** |
| `PSA_audit.v` / `audit_final2.txt` | 111→131 项（+M4c soundness） |
| `PSA_extract.v` | +FrameCheckInstance 提取 |
| `psa_guard.ml` / `psa_guard_main.ml` / `psa_guard.exe` / `psa_guard_ffi.py` | +frame 命令、+native int 镜像、24/24 |
| `PSA_M4b/T8/M4c/M15_probe.v` | 隔离探针（保留） |
| `诚实性支柱实验设计-20260819.md` | +NTK/NoPE 执行记录与判定 |
| `论文A草稿-...md` / `论文B草稿-...md` | 对齐更新（multi-seed、T8、frame、诚实性） |

---

## 八、新对话框立即行动项

1. **读本文档 + 会话 8 文档 §一（温控）**；交接瞬时 GPU **55°C（凉）**，跑训练前仍先 nvidia-smi 确认 <60°C。
2. **下会话起步（§五 顶部框）**：
   - **【选项 A】frame_check_instance_sound 主定理 (a)-(e) 装配**——**✅ 已完成（会话 10+11，审计 131→139 全零 classic）**。
   - **【选项 B】M1.5 E1 级数去 classic**——清零审计 139 项中仅剩的 4 项 classic（M1/M2 遗留），升级为"139 项全零 classic"。
   选项 B 为当前唯一剩余形式化义务，择期推进。
3. 诚实性 E1 消融（15 run，耗时长，依赖 GPU，按需推进）。
4. 论文 A/B 收尾：A §7 公理记账更新至 **139 项**、B §6 C=3 双因素结论、B §4.1 NTK 反超后"免维护+证书"叙事措辞。

*文档结束。核心：multi-seed 8/8（并列冠军 E5''/C=4 + C=3 定案）；M4 收尾①长度一致性 + T8 复合证书 + frame 提取链 + pair_sound 全落地（**审计 131 项 RC=0，框架零 Admitted**）；诚实性 NTK/NoPE 已跑（NTK 反超 21.6%、NoPE 确认位置必要）。**剩余形式化义务二选一**：① `frame_check_instance_sound` 主定理 (a)-(e) 装配；② M1.5 E1 级数去 classic（清 4 项 classic）。*
