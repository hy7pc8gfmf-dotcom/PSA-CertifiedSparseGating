# 论文 B 更新补丁：ρ^{−3/2} 三件套收官（常数演进线封顶）

> 日期：2026-08-22（z 区三件套完成后，src 侧整合）
> 依据：z/同事交互文档.md §23/§24/§25/§26；z/交接文档-20260822-z区rowsum续作.md
> 状态：**已并入论文 B 规范版 §8.4（核漂移条目）**；跨维公式数值常数实例化待接口落地。

## 1. 常数演进线（已并入规范版 §8.4 L233）

论文 B 规范版"核漂移口径（T4）"条目中的常数演进线更新为：

**4K → 2K（`psi_unconditional_basis_tight`）→ Θ(C^{−3/2})（ρ^{−3/2} 三件套）→ μ=0（网格端点）**

新增环节（机器检查，z 区 18 探针之 17/18）：
- ① 逐对 Jordan 上界：‖⟨ψ_a,ψ_b⟩‖ ≤ sin(πa/b)·√(ab)/(2(b−a))（`pair_inner_norm`，probe_pairbound）；
- ② 行和重组：任意行 ≤ 2π·C^{−3/2}/(1−C^{−3/2})（`row_sum_3halfs`），**C=4 行和 ≤ 2π/7 ≈ 0.898 < 1——1D C=4 从空洞变真实框架界**（probe_rowsum）；
- ③ 见证封顶：见证对 (2,2C) 精确内积 sin(π/(2C))/√C（`witness_exact`），且 (1−1/C)·[上界] ≤ 见证值 ≤ [上界]（`witness_sandwich`）——**上下界比 ≥ 1−1/C → 1，Θ(C^{−3/2}) 紧性封顶**（probe_witness）。

审计：三个探针均 src 侧独立编译 EXIT=0 + coqchk 内核复验通过；零 Admitted/零自定义公理；
Print Assumptions 脚印 = Dedekind 件（零 classic）。

## 2. P1 跨维公式常数改进评估（待办，不承诺）

- **方向**：ρ^{−3/2} 把行和常数从 Θ(C^{−1/2})（旧 decay_bound 逐对松 Θ(C) 因子）收紧到 Θ(C^{−3/2})；
  论文 B §6 跨维公式 M_bound^{(N)} = (C³/2)·((1+4K_C)^N − 1) 中 4K_C 的常数随之可改进。
- **现状**：组合指数增长部分已 ∀N 机器检查（probe_tensor）；**单轴常数因子 K_C 的新界实例化
  依赖 decay_bound(_tight) 接口改写（§19.4 风险②）——未完成前，跨维公式保持现有表述**，
  不把数值改进入正文。
- **不承诺**：K0=32 高维联动（§19.4 风险③）不承诺；"逐轴截断 ⟹ 外推改善"充分性仍在形式化范围外。
- **后继**：src 侧完成 decay_bound 接口实例化后，评估 M_bound^{(N)} 数值更新（诚实性：先证后写）。

## 3. 边界（不变）

- 探针归 z 区；合订版不含探针；合并版不入仓库；零 Admitted/零自定义公理；预登记判据文化。
