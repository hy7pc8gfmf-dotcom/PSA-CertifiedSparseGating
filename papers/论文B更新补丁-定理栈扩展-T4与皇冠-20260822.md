# 论文B 更新补丁：定理栈扩展（T4 并入 + 第二批 z 区 7 模块 + 皇冠）（2026-08-22）

> 状态：**待应用**（P1–P3 为可直接粘贴的替换文本；P4 为文献核查工单，须走 `参考文献真实性核查报告` 同款 DBLP/CrossRef 管线后才准入正文）。
> 来源：z 区第二批 7/7 收官（交互文档 §14.1–14.6）+ T4 softmax 组合线并入 PSA_framework（本日，coqchk 复核通过）。
> 目标文件：`论文B中文规范版-PhaseCompleteLadders.md`。

---

## 1. 本轮形式化增量（一句话版）

1. **T4 完机体落地**：`kernel_drift_controls_attention`（核逐点漂移 ≤ dc、系数 ℓ1 ≤ dd ⟹ softmax ℓ1 TVD ≤ e^{2·dc·dd}−1）+ `kernel_identical_tvd_zero`（K=K' ⟹ 界=0）已并入 PSA_framework 的 PhaseCoherence（`coherence_controls_attention` 的核漂移姊妹定理），coqchk `Modules were successfully checked`，零 classic。
2. **跨维公式从三实例升为 ∀N 定理**（probe_tensor）：off-diag 行和 ≤ (1+r)^N − 1（rpowN 递归幂），2D/3D/4D = N=2/3/4 特例——"Theoretically Composable" 从评注变定理。
3. **网格角度定理块完成**（Q3 中原"列为后续工作"项）：μ=0 全长度正交 + Parseval 能量守恒**等式** + 碰撞完备刻画（纯网格首碰撞恰 N；偏移分母 q 把碰撞推至 qN；无理 ⟹ 永不）+ Dirichlet 部分和界（窗口无关）+ 混合网格跨网格相干界 + **黄金近碰撞半径 ∀d≥1：|dφ−m| ≥ 1/(3d)**（皇冠，代数数范数路线，square5_zero 零公理）。
4. **容错分级证书**（probe_robust）：坏对占比 ≤ δ ⟹ 行和 ≤ n·(μ+δ)；μ+δ ≤ 4/5 ⟹ 通过帕累托阈值——检查器从二元升级为分级。
5. z 区累计 **13 个 Qed 模块**、0 Admitted、0 自定义公理；全部 Print Assumptions 审计（多数 Dedekind 两件，CRT/τ三分/递降核心件零公理）。

## 2. P1（§"跨维度推测"段替换建议）

原文（§6 第 6 条）称 M_bound^{(N)} 公式校验 N=2/3/4 三实例、"充分性未证"。**更新口径**：

> 组合指数增长部分已 ∀N 封死：对任意 N 轴张量积族，若逐轴 delta 行和 ≤ r_k，则 off-diagonal Gershgorin 行和 ≤ Π_k(1+r_k) − 1；均匀轴（r_k ≡ 4K_C）时为 (1+4K_C)^N − 1（机器检查，z 区 probe_tensor：`tensor_offdiag_bound` / `tensor_bound_uniform_N`）。2D/3D/4D 实例成为 N=2/3/4 特例；C³/2 单轴常数因子在实例层引入。逐轴截断的**必要性**论证不变；"充分性"（逐轴截断 ⟹ 外推改善）仍超出形式化范围。

## 3. P2（Q3 追加句）

在 Q3 答"列为后续工作方向"处追加：

> （更新 2026-08-22）该方向定理块已完成：精确正交（μ=0，训练与全部评估长度）、能量守恒等式（Parseval 恒等式，等式而非界）、碰撞距离完备刻画（碰撞 ⟺ 角度有理；有理时最小距离 = 分母闭式；纯网格恰 N；无理偏移永不碰撞）、窗口无关 Dirichlet 部分和界与混合网格跨网格相干界、以及黄金偏移的近碰撞下界 |dφ−m| ≥ 1/(3d)（近碰撞不快于 O(1/d) 聚集）。配套实验 offset-grid（θ=2π(α+m/512)，α=黄金比）3 种子进行中（预登记三比较见框架文档 §9）。

## 4. P3（论文A §5.3 / 论文B §6 交叉引用句）

> 论文 A 的注意力稳定性链现含核漂移口径：`kernel_drift_controls_attention`（PSA_framework，PhaseCoherence）——核逐点漂移 ≤ dc 且系数 ℓ1 ≤ dd ⟹ softmax 输出 ℓ1 距离 ≤ e^{2·dc·dd} − 1。对窗口无关核族（网格/偏移网格在任意 a·N 长度核相同）Δ=0，退化为 TVD 界 0（`kernel_identical_tvd_zero`）："注意力核在长度外推下的表示级不变性"成为带证书陈述的端点。常数演进线：4K → 2K（`psi_unconditional_basis_tight`）→ μ=0（网格端点）——`abstract_unconditional_basis` 可用 Riesz 序列稳定性语言重述（张成族的 (1±M_bound) 平方可和系数范数等价），该重述为文本级，不改变机器检查内容。

## 5. P4（文献核查工单——未核通过前不入正文）

| 待引 | 条目 | 核查状态（2026-08-22 联网核验） |
|---|---|---|
| YaRN | Peng, Quesnelle, et al. "YaRN: Efficient Context Window Extension of Large Language Models." ICLR 2024, arXiv:2309.00071 | ✅ 已核（标题/作者/arXiv 号/venue 全符） |
| LongRoPE | Ding, Y., et al. "LongRoPE: Extending LLM Context Window Beyond 2 Million Tokens." ICLR 2024, **arXiv:2402.13753** | ✅ 已核（**纠错：原记 2309.12307 有误**） |
| FIRE | Li, S., et al. "Functional Interpolation for Relative Positions Improves Long Context Transformers." Findings of ACL 2024, **arXiv:2310.04418** | ✅ 已核（**纠错：原记 2310.05269 有误**；Findings） |
| KERPLE | Chen, S., et al. "KERPLE: Kernelized Relative Position Embedding for Length Extrapolation." **NeurIPS 2022**, arXiv:2205.09921 | ✅ 已核（**纠错：非 ACL 2023，是 NeurIPS 2022**） |
| ~~Ruoss et al. (ACL 2023)~~ | 实为 "Learning Certified Individually Fair Representations"（公平表示，166 引）——**与本文主题（位置编码/注意力形式化）不相关，建议删除** | ⚠️ 已定论文但主题不符；若需"transformer 认证"相关工作，改引 Baader, Balunović, Vechev, "Fast and Precise Certification of Transformers"（DeepT，PoDS 2021）——条目待 DBLP 复核后再入 |

三条 ✅ 已达入库标准（arXiv 官方页 + venue 双确认），可进论文B 参考文献列表；Ruoss 删除、DeepT 替换待复核。

## 6. 关联资产

- z 区 13 模块：probe_grid_ortho / collision / tchar / parseval / partial / pairdirichlet / taudicho / kerneldrift / crt / robust / tensor / nearcoll / softmaxtvd（各 .vo 齐，合并版可并入）。
- PSA_framework.v 已含核漂移链（backup：`backup-psa-kerneldrift-20260822/`）；src 副本已同步重建。
- 实验队列：offset-grid 3 种子温控串行进行中（`baseline_ogrid_s{1337,42,7}.log`）；后续 prime-7/prime-8/E5''、τ 剪枝回测、rand-trunc-256 依序待 GPU。
