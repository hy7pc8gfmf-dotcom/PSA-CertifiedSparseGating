# 论文 A/B × 新模块（30模块）最强推理交叉分析 — 2026-08-29

> **真相源**：`D:\ComplexAnalysis\30模块`（活动权威快照，`ca_merged_full_25.v` = 57 模块 / 87588 行 / MERGE_EXIT=0，已编译出 `.vo` 11.7 MB）。
> 区别于 2026-08-22 所用的 `PSA-CertifiedSparseGating` 副本（旧 44 模块 / 78602 行）。
> **论文版本**：`.md` 中文规范版 **v2.1**（含 2026-08-23–27 补丁：RIP/τ/euler/4D/2D-wide；n=5 复核）。
> **方法**：以代码为真相源，逐条核验论文章节主张的定理语句、公理脚印、定义口径与新模块覆盖。

---

## 0. 结论速览（三角结论）

| 类别 | 条目 |
|---|---|
| **取 / Accept** | `certified_attention_approx`、`ParetoLaw` 全族、`tau_crop_mono`、`row_rip_bound_M`、`tensor_product_unconditional_basis_4d`、`euler_product` —— 机器证明成立，论文引用方向正确 |
| **改 / Revise** | **P0-1** 审计"零经典排中"被转录证伪 + 内部不一致；**P0-2** softmax 紧度常数高估（延续 08-22）；**P1-1** 引用-权威错配（探针 vs 独立模块）；**P1-2** τ 定义漂移（论文 clamp vs 代码 raw）；**P1-3** 论文B 未引 `tau_crop_mono` 加固双层 |
| **驳 / Reject** | 旧标题"可认证且外推稳健"已被 v2.1 解耦（非本次反驳，是已修正）；`ca_zeta_euler` 超两篇论文主题，若声称"完整框架"须界定范围 |

---

## 1. 取 / Accept（代码机器证明成立，论文引用正确）

1. **`certified_attention_approx`** — `PSA_framework.v:2443-2450`
   语句 `(e^{2·√ε} − 1)·Vmax` 与论文 A 引用**逐字一致**。`Proof` 链路 `softmax_l1_bound_exp → weighted_avg_lipschitz`（:2461）自洽。**✓**
2. **`ParetoLaw` 全族** — `ParetoLaw.v`
   - `c_pareto = ((5+√281)/16)²`（:75）；`pareto_law_main`（:244，3·c^(m−1)≤N）；`pareto_law_N511`（:374，m≥10 ⟹ False，经 `c9_gt_511` :350 有理下界 `(217/160)²<c` + `vm_compute`）。数学**完全正确、零 Admitted、仅 Stdlib Reals 自包含**。论文 B 引用正确。**✓**
3. **`tau_crop_mono`** — `ca_tau.v:63-65`（新模块，2026-08-23）
   `N1≤N2 ⟹ Σ_{n≤N1} τ(n) ≤ Σ_{n≤N2} τ(n)`，纯 mathcomp、零公理。裁剪单调主引理成立。**✓**
4. **`row_rip_bound_M`** — `ca_merged_full_25.fixed.v:78186`（RIP 主定理，δ=μ_row·M）
   论文 A §5.6.6 经 `probe_incoherence` 引用 RIP 链条；权威模块 `ca_rip_cr.v`（P5 收缩链 33 定理）与之对应。**✓**（见 P1-1 引用修正）
5. **`tensor_product_unconditional_basis_4d`** — `ca_basis_4d_asm.v:31`
   4D 张量积无条件基主定理，论文 A §5.4 引用 `M_bound_4d 4 = 19968` 一致。**✓**
6. **`euler_product`** — `ca_merged_full_25.fixed.v:72875`（ζ 欧拉乘积，173 定理，构造性）
   机器证明成立（但**超出两篇论文主题**，见 §3）。**✓**

---

## 2. 改 / Revise（投稿前必做）

### P0-1 审计"零经典排中"被转录证伪 + 内部不一致
- **证据 A（文件自相矛盾）**：`PSA_audit.v:1-4` 称"预期 `Classical_Prop.classic` 零出现，公理脚印仅 sig_not_dec/sig_forall_dec/fext"；但 `:110-114` 勘误承认 `softmax_ratio_le2/softmax_diff_i/softmax_l1_bound_exp` 经 `exp_increasing` 继承 `Classical_Prop.classic`；`:120-123` 同 `certified_attention_approx`。
- **证据 B（实际转录）**：`audit_run_20260826_full.txt:13-21` 首块即列 `ClassicalDedekindReals.sig_not_dec : ∀P, {~~P}+{~P}`（**双重否定消去 = 经典原则**）+ `sig_forall_dec`（依赖选择）+ `functional_extensionality_dep`；全文 `Classical_Prop` 字面计数 = **0**。
- **裁决**：
  - 论文"**全零经典排中**"被转录**证伪**——`sig_not_dec`/`sig_forall_dec` 本身即经典逻辑公理（在 Dedekind 实数下等价排中律）。
  - "**零 `Classical_Prop.classic`**"字面可辩（转录无该字符串），但 (a) 审计文件注释称继承 `Classical_Prop.classic`，与转录（仅 `sig_not_dec`）**内部不一致**；(b) `sig_not_dec` 在 `ClassicalDedekindReals` 中即由 `Classical_Prop.classic` 导出，实质等价。
- **修正**：论文须将"全零经典排中"改为诚实口径——"无显式 `Classical_Prop.classic`；仅依赖 `ClassicalDedekindReals.sig_not_dec + sig_forall_dec + functional_extensionality_dep`（Dedekind 实数基底基础设施）"，并附录完整 165 条 `Print Assumptions` 转录（现有 `audit_run_20260826_full.txt` 可直接附）。

### P0-2 softmax 紧度常数高估（延续 2026-08-22）
- **证据**：代码 `softmax_l1_bound_exp`（`PSA_framework.v:2216-2220`）= `exp(2·d) − 1`；论文 A 摘要/§1/§4 写 `2(e^d − 1)`。
- **理由**：`exp(2d)−1 > 2(e^d−1)`（d>0），论文**高估了引理紧度**。
- **附加矛盾**：代码注释 `:2248` 仍称"零 classic"，与审计转录（含 `sig_forall_dec`）冲突。
- **修正**：论文 A 三处改 `exp(2d)−1`；同步修正该注释。

### P1-1 引用-权威错配（z 区探针 vs 独立模块）
- **证据**：论文 A §5.6.6 引 `probe_incoherence`（RIP）、§5.6.4 引 `probe_taudicho`（τ）；`README.md` 明确"z 区 24 探针…**不并入合订版**（探针源含 lia/lra 且依赖 %nat 记法，合并环境 scope 污染下 lia 解析失败），权威源在 `z/`"。
- **代码权威形态**：`ca_rip_cr.v`（`row_rip_bound_M`，P5 收缩链 33 定理）、`ca_tau.v`（`tau_crop_mono`）、`ca_basis_4d`/`P1Coherence.v` 均为已入档独立模块。
- **修正**：论文应**优先引用已入档独立模块定理名**（`row_rip_bound_M` / `tau_crop_mono` / `tensor_product_unconditional_basis_4d`），探针作"独立验证（EXIT=0、零公理）"附注，避免审稿人按模块名检索不到。

### P1-2 τ 定义漂移（论文 B vs 代码）
- **证据**：论文 B §8.4 定义 `τ(n) = max(0, T_train − n)`（clamp ≥0）；代码 `ca_tau.v:23` `tau_mass T n := T − n`（**raw，可负**）。
- **影响**：OOD 区（n>T）语义不同——论文"τ≈1 负债带"/"τ=0（周期 512）"论证（2026-08-22 已指 τ≈1 vs τ=0 矛盾）须对齐代码 raw 定义，或显式定义 clamp 并证二者在 `[0,T]` 内一致。
- **修正**：统一为 raw `tau_mass` 口径，或补引理 `tau_mass_clamped_eq` 证明 `max(0,T−n)` 与 `T−n` 在判定窗口内等价。

### P1-3 论文 B 未引 `tau_crop_mono` 加固双层叙事
- **证据**：论文 B §8.4/§10.9 称"裁剪点越大保留越多（碰撞负债越多）"，代码 `ca_tau.v:63` `tau_crop_mono` 正是该形式化。
- **修正**：补引 `tau_crop_mono` 支撑"经验律 + 机器检查碰撞结构"双层叙事，回应审稿人"形式化与实证脱节"。

---

## 3. 驳 / Reject（代码证伪或超出范围）

1. **旧标题强主张** —— 论文 B v2.1 标题已从"可认证且外推稳健的位置编码"改为 **"相位截断频率阶梯：带证书的基稳定性核的受控实证研究"**（A Controlled Empirical Study with a Certified Basis-Stability Core）。**2026-08-22 P0 标题解耦建议已落地**，非本次反驳。
2. **`ca_zeta_euler.v`（ζ 欧拉乘积，173 定理）** —— 机器证明成立，但属 PCT/数论线，**超出论文 A/B 主题**。若论文声称"完整形式化框架"须界定范围；否则非缺口。

---

## 4. 对 2026-08-22 分析结论的漂移

| 2026-08-22 结论 | v2.1 状态 | 裁决 |
|---|---|---|
| 论文 B 标题"可认证且外推稳健"强主张（P0） | 标题已解耦为"带证书的基稳定性核的受控实证研究" | **✅ 已修正** |
| 统计 n=3 / 评估仅 32k 字符（P0） | §10.9 引入 n=5 复核（2026-08-24）、tinystories200+27000 iters、τ 裁剪多种子降格 | **⚠️ 部分回应**（需核评估协议 n_batches≥50 是否落实，见 §5） |
| 审计"零经典排中"（P0） | 未改；本次核验确认转录仍含 sig_not_dec | **⚠️ 未修正** |
| softmax 紧度常数 `2(e^d−1)`（P0） | 未改；代码仍为 `exp(2d)−1` | **⚠️ 未修正** |
| 缺 `_CoqProject`/Makefile（P1） | 30模块 **已有 `_CoqProject`**（346B）；合并版 `.vo` 已编译 | **✅ 已补**（副本库旧判据不适用新模块） |

---

## 5. 诚实声明的残余不确定性

- **本环境无 `coqc`**，未重编译验证；结论基于 (a) `ca_merged_full_25.vo` 已编译（11.7 MB，README 声明 MERGE_EXIT=0）与 (b) 源码精读。定理语句以源码为准。
- **未核验项（需另轮）**：
  1. 论文 B **摘要**是否仍残留"可认证且外推稳健"（标题已改，摘要待查）；
  2. 实验评估协议 `length_extrap.py` 是否已按 2026-08-22 建议改 `n_batches≥50`+固定 RNG+报告 CI；
  3. 论文 A 是否已引用 `ca_rip_cr.v` 权威模块（当前仅见探针名）。
- `ca_merged_full_25.fixed.v` 为后处理版本（`.fixed` 后缀），**权威以独立模块 + 合并版 `.vo` 为准**；grep 引用其行号仅作定位。
- 审计转录 `audit_run_20260826_full.txt` 文件头显示旧路径 `PSA-CertifiedSparseGating\coq\core\PSA_audit.v`（stale 注释），但其内容（165 块、sig_not_dec 遍布）即 30模块 `PSA_audit.v` 的运行输出，实质有效。

---

## 6. 投稿前修订清单（P0/P1）

**P0（阻塞）**
1. 审计口径：删"全零经典排中"→ 诚实列 `sig_not_dec/sig_forall_dec/fext`，附 165 条转录。
2. softmax 紧度：论文 A 摘要/§1/§4 改 `exp(2d)−1`。

**P1（重要）**
3. 引用-权威错配：论文 A 优先引 `row_rip_bound_M`/`tau_crop_mono`/`tensor_product_unconditional_basis_4d`，探针降级附注。
4. τ 定义漂移：论文 B §8.4 对齐代码 raw `tau_mass` 或证 clamp 等价。
5. 论文 B 补引 `tau_crop_mono` 加固双层叙事。
6. 自查论文 B 摘要是否残留旧强主张（标题已改）。

**已落地（无需重复）**：标题解耦、n=5 复核、`_CoqProject` 存在。
