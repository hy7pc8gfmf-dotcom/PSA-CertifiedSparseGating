# 交叉核验总表 · 论文 A ↔ 论文 B ↔ 代码（独立投稿一致性）

> 两文独立投稿（A→CPP/ITP，B→TACL），共享同一阶梯族与代码基态。本表汇总**共享声明的一致性**、**单一根因**、**耦合风险**与**统一修正建议**。

## 1. 共享声明一致性矩阵

| 共享声明 | 论文 A | 论文 B | 代码实测 | 一致？ |
|---|---|---|---|---|
| 合并版文件名/规模 | `ca_merged_full_25.v` / 87733 行 / 55 模块 | `ca_merged_full_24.v` / 75702 行 / 40 模块 | `ca_merged_full_25.v` / 87733 / **57** 顶层 | ⚠ **B 引用旧快照**（P1） |
| C=4 实例证书 μ=4/5，[1/5,9/5] | ✓ | ✓ | 0.312072，成立 | ✓ |
| E5'' 行和 1.2287，检查器真阴性 | ✓ | ✓ | 1.228720 | ✓ |
| T8 核 [3,15,63,255] 可认证 | ✓ | ✓ | 0.2994<0.8 | ✓ |
| `champion_e5_composite_certificate`，coh_e5=14.21，下界−7.21 | ✓ | ✓ | 14.2119 / −7.2119 | ✓ |
| 反射检查器 `frame_check_instance_sound` 健全性 | ✓ | ✓ | 合并版命中 | ✓ |
| 酉不变性 `unitary_invariance_psi_rope_theta` | ✓ | ✓ | 合并版命中 | ✓ |
| `M_bound` 768/3968/19968 | ✓ | ✓ | 实测一致 | ✓ |
| 165 项审计 RC=0、零 classic | ✓ | ✓ | `PSA_audit.v` 已补交，165 目标 165/165 在合并版存在；文件 L108–121 勘误为 pre-M1.5 过时记录；post-M1.5 静态核验支持 165/165 零 classic；现有 `audit_run.txt` 55 块全零 classic，但仅捕获 55/165、未随稿 | ✓ **post-M1.5 成立；完整日志须补**（P1） |
| 42 处 Classical 全在 `ca_*` 基础设施，应用层零 classic | ✓ | ✓ | 42 处实测，PSA_framework=0 | ✓ |
| 证书边界（不覆盖 logits/PPL/外推） | ✓（§1/§7/§14） | ✓（§7/§8） | 代码层面一致 | ✓ 双向正交声明 |
| `golden_near_collision_gold` 零公理 | ✓（§8） | ✓（§8） | `probe_nearcoll.v`（z 探针） | ✓（索引未列文件名 P3） |

## 2. 单一根因（single root cause）

> **根因 R1：快照基态漂移 + 工件随稿粒度不一致 + "零 classic"口径经 post-M1.5 已闭合。**
> 论文 B 在写作时冻结于旧基态 `ca_merged_full_24.v`（75702 行 / 40 模块 / 7 探针），而本次提交与论文 A 均基于 `ca_merged_full_25.v`（87733 行 / 57 模块 / 20 探针）。同时，165 项审计、OCaml/FFI 提取、构建脚本（`_CoqProject`/`Makefile`/`_merge_ca.py`）未以 artifact 粒度随合并版 `.v` 提交，且 `PSA_audit.v`（用户已于 2026-08-26 16:19 补交）是一份**脚本**，配套 `audit_run.txt`（`Live_harness\AI注意力算法\审计证据\` 与 `PSA-CertifiedSparseGating\docs\`，Aug 21）现存在但其**仅捕获 55/165 个定理块、未随稿 co-locate** → "零 classic / RC=0"的**完整机器证据仍待补交完整 165 项日志**。关于 `PSA_audit.v` L108–121 自带勘误曾记载认证注意力近似链（M1+M2，4 项）继承 `Classical_Prop.classic`——**该勘误为 pre-M1.5（2026-08-19）过时记录**：当前 `ca_merged_full_25.v` 已并入 `ExpSeries.exp_mono_le_noclassic`（L63579/L63672/L63719），4 个注意力定理位于其后，`exp_increasing` 仅存于 `ca_*` 基础设施（非审计目标）→ **post-M1.5 静态核验支持「165/165 零 `Classical_Prop.classic`」**。
> 该根因同时解释：① 论文 B 版本引用失配；② 模块计数三数不一（A:55 / B:40 / 实测:57）；③ 165 项审计"完整日志缺 + L108–121 勘误过时"。三者同源，统一修正即可（详见 `补遗-PSA_audit核查.md`）。

## 3. 耦合风险（独立投稿视角）

- **C1（版本耦合）**：两文若不同步到 25，reviewer 交叉比对时会发现 B 引用文件与 A/提交的仓库不符，质疑一致性。→ 两文统一引用 `ca_merged_full_25.v`。
- **C2（审计工件耦合 + classic 声明耦合）**：两文均依赖同一 `PSA_audit.v`（165 项）。该文件已补交（165/165 目标在合并版存在）；其 L108–121 勘误为 pre-M1.5 过时（4 项 classic 已由 M1.5 `exp_mono_le_noclassic` 消除，`ca_merged_full_25.v` L63579/L63672/L63719）；现有 `audit_run.txt` 仅捕获 55/165 块且未随稿。→ 两文"165/165 零 classic"声明 post-M1.5 成立，但须随稿补**完整 165 项** `audit_run.txt`+构建配置使可复跑，并将 L108–121 勘误标注为 pre-M1.5 过时。
- **C3（数值口径耦合）**：B 的实证 `Coh(T)`（窗口相关、0.0317）易被读者与 A 的证书 Gram（窗口无关、0.149/0.312）混淆。两文已声明正交，但 B §9.1 仍须显式区分（P2）。
- **C4（主张正交性已声明，非风险）**：A=表示稳定性，B=外推 PPL，无形式化桥梁——两文双向声明一致，不构成冲突，是可审计 AI 定位的正确边界。

## 4. 统一修正清单（两文共须）

| 严重度 | 修正 | 影响论文 |
|---|---|---|
| **P1** | 论文 B 全文 `ca_merged_full_24.v` → `ca_merged_full_25.v`，同步行数(87733)/模块(57)/探针(20) | B |
| **P1** | 论文"165 项全部零 classic / 应用层零 classic"口径 post-M1.5 成立（M1.5 `exp_mono_le_noclassic` 已消除 `exp_increasing`→classic 入口；`PSA_audit.v` L108–121 为 pre-M1.5 过时勘误）；须显式声明 `sig_forall_dec`/`sig_not_dec`/`fext` 属可接受 Reals 基底、非 `Classical_Prop.classic` 公理；并随稿补**完整 165 项** `audit_run.txt`+`_CoqProject`/`Makefile`/`_merge_ca.py`（现有日志仅 55/165 块、未 co-locate） | A+B |
| **P1** | artifact 补齐 `audit_run.txt`+`_CoqProject`/`Makefile`/`_merge_ca.py`（使 165 项审计可复跑；`PSA_audit.v` 已补交，无需再补） | A+B |
| **P2** | 论文 B §9.1 显式区分 `Coh(T)` 与证书 Gram 相干（不同对象）；附 `coherence_analysis.py` 或注明同源 | B |
| **P3** | 模块计数统一为实测 57（A 的 55、B 的 40 一并修正） | A+B |
| **P3** | 声明计数注明口径（269=Lemma+Theorem vs 316 顶层声明/272 Qed） | A |
| **P3** | 附录 A 交叉索引补 `probe_nearcoll.v`（承载 `golden_near_collision_gold`） | A+B |

## 5. 核验结论摘要
- **数值正确性**：C=4 / M4 / T8 / E5'' / coh_e5 / M_bound / K0′ 全部经双方法独立复核**通过**，偏差 ≤1e-4（行和 μ）至 1e-15（Dirichlet 闭式 vs 暴力）。
- **诚信纪律**：零 Admitted、零 Abort、42 处 `Require Import Stdlib.Logic.Classical` 全在 `ca_*` 基础设施、`PSA_framework.v` 内为 0——**结构扫描确认成立**；核心证书（C=4/E5''/T8/frame_check/Gershgorin/Dirichlet）确零 `Classical_Prop.classic`。**post-M1.5 静态核验表明全部 165 项均零 `Classical_Prop.classic`**（`exp_increasing`→classic 唯一入口已由 M1.5 `exp_mono_le_noclassic` 消除，`exp_increasing` 仅存于 `ca_*` 基础设施、非审计目标；`PSA_audit.v` L108–121 勘误为 pre-M1.5 过时记录）→ "165 项全部零 classic"声明 post-M1.5 成立。
- **硬缺口（post-M1.5 已实质性闭合）**：`PSA_audit.v` 已补交且其 165 目标 165/165 在合并版存在；配套 `audit_run.txt` 现存在（Live_harness + PSA-CertifiedSparseGating/docs，Aug 21），其 55 个定理块全零 `Classical_Prop.classic`，但仅捕获 55/165、未随稿 co-locate → "零 classic / RC=0"的**完整机器证据仍待补交完整 165 项日志**；"全部零 classic"声明经 post-M1.5 静态核验**成立**（L108–121 勘误为 pre-M1.5 过时）。核心定理本身可证、数值已独立复核。
- **唯一版本错误**：论文 B 引用旧快照 24（P1），须统一为 25。
- 其余为计数口径（P3）与归一化澄清（P2）类修善，不阻塞投稿。
