# 合并报告：22+2 模块 → 自包含单文件（2026-08-19）

> **任务**：对比 `lib-构造性/` 与 `src/` 的 22 个模块，以 `src/` 为基准合并为自包含单文件，
> 并评估将 `ca_basis_3d.v`（23）与 `PSA_framework.v`（24）并入。
> **规则**：`src/AGENTS.md` 硬规则 3（合并版剔除全部 `Require Import ca_*`）+ 技能
> `.zcode/skills/coq-live-repair/SKILL.md` §3（分区逐字 + LF 行尾 + 再生后零残留验证）。

---

## 一、对比结论：22/22 完全一致 ✅

对 `lib-构造性/` 与 `src/` 的 22 个 `ca_*.v` 逐文件 SHA-256 对比：

- **22/22 哈希完全一致**（含 ca_base / ca_basis_lemmas / ca_gamma / ca_decay 等大文件）；
- 两目录内容相同——`lib-构造性`（构造性轨道快照）与 `src`（3D 构建工作区依赖）未分叉；
- `src/ca_basis_3d.v` 与 `AI注意力算法/ca_basis_3d.v` 哈希也一致（同一最新版）。

## 二、基准决策：以 `src/` 为基准 ✅

理由：① 与 `lib-构造性` 内容完全一致（无需二选一）；② `src` 是 3D 模块的构建工作区
（含全部 .vo 与探针），`ca_basis_3d` 已在其中；③ 合并产物放在 `src`，与拆分说明、
合并分析、AGENTS.md 同目录，便于 Windows 本地调用。

## 三、合并执行（脚本 `src/_merge_ca.py`，可复现）

| 规则 | 落实 |
|------|------|
| 剔除全部 `Require Import ca_*` | 50 行被剥除（20 文件×1 + ca_zeta_honest×20 + ca_basis_3d×9 + PSA_framework×1）；**零残留**（grep 验证 = 0） |
| 分区逐字复制 | 每个模块分区 = 源文件逐字内容（内部 Stdlib/Coquelicot/mathcomp Require 保留，含重复导入——与既有合并版惯例一致） |
| LF 行尾 | 产物 CR 字符数 = 0（源 CRLF → LF） |
| DAG 顺序 | 脚本校验：每模块的 ca_* 依赖 ⊆ 前序模块（0 违规） |

**产物**（均在 `src/`）：

| 文件 | 模块数 | 规模 | 内容 |
|------|--------|------|------|
| `ca_merged_full_23.v` | 23 | 2,267,786 B / 58,449 行 | 22 个 ca_* + ca_basis_3d |
| `ca_merged_full_24.v` | 24 | 2,456,801 B / 62,881 行 | 23 + PSA_framework（第 24 模块） |

**本地编译命令**（自包含，无需 -Q lib）：
```powershell
& "C:\Rocq-Platform~9.0~2025.08\bin\coqc.exe" `
  -Q "C:\Rocq-Platform~9.0~2025.08\lib\coq\user-contrib\mathcomp" mathcomp `
  -Q "C:\Rocq-Platform~9.0~2025.08\lib\coq\user-contrib\Coquelicot" Coquelicot `
  "D:\ComplexAnalysis\Live_harness\src\ca_merged_full_24.v"
```
（约 10 分钟；编译验证由用户执行——本会话已在后台启动 24 版编译作为参考。）

## 四、PSA_framework 并入可行性分析（第 24 模块）✅

**结论：技术上完全可行，已并入为第 24 模块。**

- **依赖闭合**：`PSA_framework.v` 仅依赖 `ca_base ca_complex_foundation ca_independence
  ca_basis ca_basis_lemmas ca_decay`（6 个）——全部在 22 模块内，剥除其单行
  `Require Import ca_*` 后自包含成立；
- **模块名无冲突**：PSA 的 13 个 Module（RuntimeGuards…FrameCheckInstance）与 ca_* 的
  Module（ComplexNumbers / ExtendedTheorems / UnconditionalBasisLemmas 等）零重名；
- **顶层定义无冲突**：PSA 全部定义在 Module 内（命名空间化），不重定义 psi/Csum 等；
- **Import 可解析**：`Import ComplexNumbers.` / `Import ExtendedTheorems.` /
  `Import UnconditionalBasisLemmas.` 在合并文件内均指向前序分区定义 ✓。

**边界（如实声明）**：
1. 合并版是**派生便利产物**，不是权威源——AGENTS.md 明确 `lib/`（此处为 `src/` 拆分模块）是唯一权威源；合并版不在其上手工修改；
2. PSA_framework 的**独立审计（139 项）与提取链（psa_guard）不受影响**——并入只改变"调用形态"，不改变其内容与证据链；
3. 若未来 24 版编译失败，先隔离 PSA 分区排查（`ca_merged_full_23.v` 不受影响）。

## 五、模块计数说明

- 正式 ca_* 库：**22**（lib-构造性 = src，哈希一致）；
- 加入 3D 模块：**23**（ca_basis_3d，依赖全部在前序）；
- 再加入算法模块：**24**（PSA_framework）——严格说 PSA 是"派生自该代码库的算法模块"，
  "第 24 个模块"指合并文件中的分区序号，而非 ca_* 库族成员。

## 六、PSA_framework 依赖闭包（会话 12 分析）

- **直接依赖 6 个**：ca_base, ca_complex_foundation, ca_independence, ca_basis,
  ca_basis_lemmas, ca_decay；
- **传递闭包 = 21 个**（22 个 ca_* 模块中**仅排除 ca_zeta_honest**）——因 ca_decay 传递
  依赖几乎全部前序；
- **自包含判定**：PSA_framework 无法脱离 21 个前置模块单独自包含（其全部解析核依赖
  ca_*）；但 **"前置子集合并"可行** = 21 模块 + PSA = 22 模块单文件（不含 ca_zeta_honest）。
  本会话已按"全量合并"交付（24 模块版，含 ca_zeta_honest 与 ca_basis_3d）；若需要最小化，
  可再生成 21+PSA 的 22 模块版（脚本参数化，秒级再生）。

## 七、弃用警告修复（会话 12，36 处）

按 `_merge24_compile.log` 分类（deprecated 类）：

| 类别 | 修复 | 处数 |
|------|------|------|
| deprecated-missing-stdlib（裸 `Require Import X.`） | → `Require Import Stdlib.<path>.`（路径全部探针验证；RealField 实为 `Stdlib.setoid_ring.RealField`） | 33 |
| deprecated-syntactic-definition（`Rinv_mult_distr`） | → `Rinv_mult`（8.16 起，且更强：无非零前提，调用点去掉假设参数） | 3 |

- 涉及 12 个模块（ca_taylor 10、ca_fourier 7、ca_zeta_scaffold 4、ca_complex_analysis 3、
  ca_gamma 3、ca_algebra 2、ca_decay 2，其余各 1）；
- 备份：`src/backup-deprecation-20260819/`（23 文件）；
- 非弃用类警告（require-in-module 152 / notation-overridden 26 / hiding-delimiting-key 13 /
  require-in-section 13 / ambiguous-paths 5）为脆弱/解析类，非 deprecated，保留不动。

## 八、合并版编译阻塞与修复（会话 12，全部解决 → ca_merged_full_24.v RC=0 ✅）

合并单文件环境与独立编译环境的差异导致 PSA/ca_basis_3d 内容在合并中暴露 4 类系统性
elaboration/作用域问题，全部已在**源文件**修复（独立编译与合并编译双通过）：

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 1 | ca_basis_3d `phi_flat_decay_general_3d` 裸 `eq_refl` 参数 elaboration 失败 | Rocq 9 `eq_refl : Reflexive Logic.eq` 作位置参数在全局命名空间环境的展开不稳（转换本身 OK——reflexivity 可证） | 6 个裸 eq_refl 改 `reflexivity` 显式假设；`one_le_half_K0_dprod` 改 `apply` 前 3 参 + reflexivity |
| 2 | PSA `<=?`/`<?` 解析到字符串比较、`c_sparse_subset` 解析到 ca_probabilistic 的 1 参版 | 合并全局命名空间名字/记法冲突（`<=?` 需 nat_scope；`c_sparse_subset` 双定义） | `<=?`/`<?` 全部 `%nat` 显式化（39 处）；PSA 顶部 `Import UnconditionalBasisLemmas.` 显式后导入 3 参版 |
| 3 | PSA `match` 的 `0` 模式、`=?` 等无解释；未注解 R 表达式（`INR C - 1`）归错 scope | 合并环境无隐式作用域 | `| 0 =>` → `| 0%nat =>`（2 处）；`change 1 with (INR 1)` → `change (1%R) with (INR 1)`（2 处）；**PSA 顶部 `Open Scope nat_scope.` + `Open Scope R_scope.`（R 后开）——系统性修复** |
| 4 | `Import PSA_framework.GreedyGate` 等限定名失效 | 合并文件中 PSA 的 Module 是顶层（无 `Module PSA_framework` 包裹），限定名解析失败 | `Import PSA_framework.X` → `Import X`（16 处） |

**最终验证**：`ca_merged_full_24.v` 编译 **RC=0**（本会话后台复核）；`ca_merged_full_23.v` 为其前缀
（23 个分区均已随 24 编译通过）。src 23 模块全量 .vo 重建进行中（本会话后台）。

*文档结束。合并脚本 `_merge_ca.py`、弃用修复 `_fix_deprecations.py`、PSA 记法修复 `_fix_psa_le.py`
均可复现；产物 `ca_merged_full_23.v` / `ca_merged_full_24.v` 自包含、零 ca_* Require、
零裸 Stdlib Require、LF 行尾、弃用警告清零（仅剩非弃用的 fragile/parsing 类）。*
